export class Movie{
   poster: string;
   title: string;
   code: string;
   
   constructor(poster: string, title: string, code: string){
       this.poster = poster;
       this.title = title;
       this.code = code;
   }
}